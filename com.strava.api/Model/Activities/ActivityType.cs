namespace com.strava.api.Model.Activities
{
    public enum ActivityType
    {
        Unknown,
        Ride,
        Run
        // TODO: CG to complete... Strava has many more activity types. Can these be queried via the API rather than maintain them in an enum? Either way, this list needs to be extended.
    }
}
